package BatchSet;

=head1 Name

   BatchSet.pm   A module for the db interface for sets of batch thingies.

=head1 Usage

   use BatchSet;
   $batchSet = new BatchSet([options]);

=cut

use strict;
use Pelement;
use PCommon;
use DbObjectSet;

1;
