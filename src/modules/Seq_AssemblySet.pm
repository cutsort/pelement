package Seq_AssemblySet;

=head1 Name

   Seq_AssemblySet.pm   A module for the db interface for sets of sequence assembly thingies.

=head1 Usage

   use Seq_AssemblySet;
   $seqASet = new Seq_AssemblySet([options]);

=cut

use strict;
use Pelement;
use PCommon;
use base 'DbObjectSet';

1;
